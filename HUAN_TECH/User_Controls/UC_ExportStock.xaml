<UserControl x:Class="HUAN_TECH.User_Controls.UC_ExportStock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HUAN_TECH.User_Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Width" Value="150"/>
            </Style>

            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Width" Value="150"/>
            </Style>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WrapPanel>
            <ComboBox DropDownClosed="txt_commodityGroup_DropDownClosed" Name="txt_commodityGroup" materialDesign:HintAssist.Hint="Group Name"/>
            <ComboBox Name="txt_commodityName" materialDesign:HintAssist.Hint="Commodity Name"/>
            <TextBox Name="txt_quantity" materialDesign:HintAssist.Hint="Quantity"/>
            <Button Click="Event_AddCart" Content="{materialDesign:PackIcon Kind=CartPlus}" Width="80" Margin="2" ToolTip="Thêm vào giỏ hàng"/>
            <Button Click="Event_InvoicePrinting" Content="{materialDesign:PackIcon Kind=Printer}" Width="80" Margin="2" ToolTip="In hóa đơn"/>
        </WrapPanel>

        <DataGrid Name="dtg_exportstork" IsReadOnly="True" Grid.Row="1" Margin="10,0,-10,0">
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <Button Content="{materialDesign:PackIcon Kind=Delete, Size=11}" Height="18"/>
                            </WrapPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>                    
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Mã sản phẩm"/>
                <DataGridTextColumn Header="Tên sản phẩm"/>
                <DataGridTextColumn Header="Đơn giá"/>
                <DataGridTextColumn Header="Số lượng"/>
                <DataGridTextColumn Header="Thành tiền"/>
                <DataGridTextColumn Header="Thời gian bảo hành"/>
                <DataGridTextColumn Header="Ngày mua"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
